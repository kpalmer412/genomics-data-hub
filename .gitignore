# Terraform state — never commit local state to GitHub
terraform.tfstate
terraform.tfstate.backup

# Terraform directory — contains provider binaries (100MB+)
.terraform/

# Lock file — DO commit this one (removed from ignore)
# .terraform.lock.hcl

# Sensitive variable files
*.tfvars
*.tfvars.json

# Mac junk
.DS_Store

# Crash logs
crash.log
crash.*.log
